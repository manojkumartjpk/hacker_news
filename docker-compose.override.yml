services:
  frontend:
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules

  backend:
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
